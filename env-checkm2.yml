# ==============================================================================
# PT-BR: Ambiente isolado do CheckM2. Ele exige Python 3.12, incompatível com o
#        ambiente principal (Python 3.11). Ver comentário em environment.yml.
# EN-US: Isolated CheckM2 environment. It requires Python 3.12, incompatible with
#        the main environment (Python 3.11). See the note in environment.yml.
# ==============================================================================
name: checkm2
channels:
  - conda-forge
  - bioconda
dependencies:
  - checkm2
